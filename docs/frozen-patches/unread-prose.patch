diff --git a/kullback/gates/fidelity.py b/kullback/gates/fidelity.py
index 6c89b40..27c198b 100644
--- a/kullback/gates/fidelity.py
+++ b/kullback/gates/fidelity.py
@@ -20,6 +20,7 @@ from typing import Any
 from kullback.gates.tool_runs import compare_results
 from kullback.runner.gate_support import MISS_REASONS, _get, _rate, _same, gate
 from kullback.runner.records import GateResult
+from kullback.runner.replay import unread_of
 
 # --- the per-tool bar (compile_tools gate 5, D80) ---
 
@@ -41,6 +42,21 @@ def replay_match(call: Any, canon_rules: Any = None, schema: Any = None, readers
     return _same(_get(call, "expected"), _get(call, "actual"), canon_rules=canon_rules)
 
 
+def _unread_body_call(call: Any, readers: Any = None) -> bool:
+    """Whether this recorded call is unread prose, so the body score must not count it (D248)."""
+    if _get(call, "expected_error") is not None:
+        return False
+    tool = str(_get(call, "tool", "") or "")
+    return unread_of(_get(call, "expected"), _get(call, "actual"), readers=readers, tool=tool)
+
+
+def _unread_unmeasured(unread: int, totals: dict) -> list[str]:
+    """A ruling that saw only unread calls measured nothing, which is not a pass (D248)."""
+    if unread and not totals["success"][0] and not totals["error"][0]:
+        return ["every recorded call is unread prose: this ruling measured nothing"]
+    return []
+
+
 def replay_fidelity_gate(calls, canon_rules: Any = None, schema: Any = None,
                          readers: Any = None) -> GateResult:
     """Gate 5: replay of recorded calls, success and error fidelity reported separately (R22 item 8, D80).
@@ -48,17 +64,24 @@ def replay_fidelity_gate(calls, canon_rules: Any = None, schema: Any = None,
     `canon_rules` is the customer's canonicalization: a gate given none compares under the module
     defaults and can call two values different that the Verdict calls the same (gate_support.py).
     `schema` and `readers` are threaded the same way and for the same reason: without them this
-    ruling holds every column to a hard column's bar (D187).
+    ruling holds every column to a hard column's bar (D187). Unread prose that did not
+    match is reported on `differs_by_unread` and skipped in the pass/fail numerator
+    (D248). Bytes-equal and canonical-equal prose still count as matched.
     """
     failures, misses, per_tool = [], [], {}
     totals = {"success": [0, 0], "error": [0, 0]}
+    unread = 0
     for call in calls or ():
         kind = "error" if _get(call, "expected_error") is not None else "success"
         tool = _get(call, "tool", "?")
+        matched = replay_match(call, canon_rules, schema, readers)
+        if not matched and _unread_body_call(call, readers):
+            unread += 1
+            continue
         slot = per_tool.setdefault(tool, {"success": {"total": 0, "matched": 0}, "error": {"total": 0, "matched": 0}})
         totals[kind][0] += 1
         slot[kind]["total"] += 1
-        if replay_match(call, canon_rules, schema, readers):
+        if matched:
             totals[kind][1] += 1
             slot[kind]["matched"] += 1
             continue
@@ -69,9 +92,11 @@ def replay_fidelity_gate(calls, canon_rules: Any = None, schema: Any = None,
             failures.append(f"{tool}: a recorded {kind} call replays differently; D80 wants 100% on recorded calls")
         elif reason not in MISS_REASONS:
             failures.append(f"{tool}: a held-out {kind} call misses with no reason ({reason!r})")
+    failures += _unread_unmeasured(unread, totals)
     metrics = {kind: _rate(totals[kind][0], totals[kind][1], [m for m in misses if m["kind"] == kind])
                for kind in ("success", "error")}
     metrics["per_tool"] = per_tool
+    metrics["differs_by_unread"] = unread
     return gate("compile_tools.replay_fidelity", failures, **metrics)
 
 
@@ -89,6 +114,9 @@ def summarize(replays: dict[str, dict[str, dict]]) -> dict:
     body left where they were, and how many later calls parted over a row an earlier write had left
     stale. A round reads the third against the second: a corpus where they move together is one
     where the reads that failed were never the reads' own fault.
+
+    `differs_by_unread` is D248's: prose results nobody read into a column, counted apart from
+    token set and presence, and not used to keep or beat a body.
     """
     rows = [r for per_task in replays.values() for r in per_task.values()]
     tasks_confirmed = sum(any(r["confirmed"] for r in per_task.values()) for per_task in replays.values())
@@ -113,7 +141,9 @@ def summarize(replays: dict[str, dict[str, dict]]) -> dict:
             # second is the number that says whether the judging is wired: a corpus with semantic
             # columns and nothing but unresolved checks has no semantic comparison happening.
             "differs_by_judge": total("differs_by_judge"),
-            "differs_unresolved": total("differs_unresolved")}
+            "differs_unresolved": total("differs_unresolved"),
+            # Prose nobody read into a column, counted apart from token set and presence (D248).
+            "differs_by_unread": total("differs_by_unread")}
 
 
 def unconfirmed_reason(per_task: dict[str, dict]) -> str:
diff --git a/kullback/runner/replay.py b/kullback/runner/replay.py
index 1b35031..7ed590f 100644
--- a/kullback/runner/replay.py
+++ b/kullback/runner/replay.py
@@ -66,10 +66,15 @@ BY_COLUMNS, BY_TOKEN_SET, BY_PRESENCE, BY_VALUE, BY_ERROR = "columns", "token_se
 # anyone found, it is a question nobody answered, and a build cannot repair what it cannot name
 # (D219).
 BY_UNRESOLVED = "unresolved"
+# A recorded result that is still a sentence, with no reader that bound a column of it. The two
+# sides part, but not as a hard column miss: nobody read the sentence into columns, so compile_tools
+# cannot score the body on it (D248). Unread is never agreement.
+BY_UNREAD = "unread"
+UNREAD_NOTE = "unread:"
 # The meaning of a recorded verdict. Bump it where that meaning changes, so a cached replay written
 # under the older rule is recomputed rather than read back (D217): the stage's code version carries
 # it, beside the hashes of the modules the scoring is done by.
-VERDICT_FORMAT = 3
+VERDICT_FORMAT = 4
 
 
 class _Script:
@@ -396,6 +401,33 @@ def _route_of(agreed: bool, notes: Iterable[str]) -> str:
     return BY_EXEMPT if any(note.startswith(EXEMPT_NOTE) for note in marks) else BY_COLUMNS
 
 
+def _is_prose(value: Any) -> bool:
+    """A result that stayed a sentence after `_norm`, so it is not a row and not JSON."""
+    return isinstance(value, str) and bool(value.strip())
+
+
+def _reader_bound(readers: Any, tool: str) -> bool:
+    """Whether this tool has a reader that can bind a column of its result."""
+    if readers is None or not tool:
+        return False
+    table_of = getattr(readers, "table_of", None)
+    return callable(table_of) and table_of(tool) is not None
+
+
+def unread_of(theirs: Any, ours: Any, *, readers: Any = None, tool: str = "") -> bool:
+    """True when both answers are prose and this tool has no reader (D248).
+
+    A sentence nobody parsed cannot be scored as a hard column miss, and it is not agreement
+    either. A reader that is registered still scores the call, even when one side fails to
+    parse: that is a miss, not unread. The callers that keep or beat a body skip unread
+    differs, and replay records them on their own route.
+    """
+    left, right = _norm(theirs), _norm(ours)
+    if not (_is_prose(left) and _is_prose(right)):
+        return False
+    return not _reader_bound(readers, tool)
+
+
 def compare_call_route(recorded: ToolCall, result: Any, error: Any, rules: Any = None,
                        comparer: Any = None) -> tuple[str, list[str], str]:
     """The verdict, what parted, and the route the verdict was reached by (D187, D217).
@@ -408,7 +440,12 @@ def compare_call_route(recorded: ToolCall, result: Any, error: Any, rules: Any =
     of keyed rows is paired by key rather than by position. Two answers that agree there agree
     cosmetically: the effect is the same and the bytes are not.
 
-    Given no comparer the two old routes stand alone, which is what every caller had before.
+    A fourth path sits in front of the comparer when both sides are still prose and no reader bound
+    a column: the verdict is differs by unread, not same and not a column miss, so a body is not
+    failed for a sentence nobody could score (D248).
+
+    Given no comparer the two old routes stand alone, which is what every caller had before, except
+    unread prose, which is unread with or without a comparer.
 
     The route is kept beside the verdict, because a cosmetic verdict reached on the bytes and one
     reached because a judge called two sentences equivalent are not the same evidence that the
@@ -427,6 +464,9 @@ def compare_call_route(recorded: ToolCall, result: Any, error: Any, rules: Any =
         return SAME, [], BY_BYTES
     if canonicalize(ours, rules) == canonicalize(theirs, rules):
         return COSMETIC, [], BY_CANONICAL
+    readers = getattr(comparer, "readers", None) if comparer is not None else None
+    if unread_of(theirs, ours, readers=readers, tool=recorded.name):
+        return DIFFERS, [f"{UNREAD_NOTE} no reader bound a column"], BY_UNREAD
     if comparer is None:
         return DIFFERS, [], BY_VALUE
     agreed, notes = comparer.agrees(recorded.name, theirs, ours)
@@ -611,6 +651,9 @@ def _score(trace: Trace, state: Any, scored: ScoredRouter, script: _Script, mode
         "differs_by_presence": _by_route(scored.checks, DIFFERS, BY_PRESENCE),
         "differs_by_judge": _by_route(scored.checks, DIFFERS, BY_JUDGE),
         "differs_unresolved": _by_route(scored.checks, DIFFERS, BY_UNRESOLVED),
+        # Prose nobody read into a column: counted here, never as a token set or a presence miss
+        # and never as a body keep-or-beat (D248).
+        "differs_by_unread": _by_route(scored.checks, DIFFERS, BY_UNREAD),
     }
     # The route is part of the reason: "differs" alone does not say whether the two answers were
     # made of different domain tokens, held different columns, or simply parted at a value (D217).
@@ -638,4 +681,4 @@ def _score(trace: Trace, state: Any, scored: ScoredRouter, script: _Script, mode
 # The per-Task ruling over these records (some Trace confirmed, or why none did) is
 # `kullback.gates.fidelity.reference_replay_gate`; this module scores one replay and stops.
 __all__ = ["Replay", "ScoredRouter", "TraceModel", "TraceUser", "compare_call", "compare_call_notes",
-           "compare_call_route", "difference", "effect_reasons", "replay_trace"]
+           "compare_call_route", "difference", "effect_reasons", "replay_trace", "unread_of"]
